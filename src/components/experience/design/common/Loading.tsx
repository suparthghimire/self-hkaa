import { Center } from "@mantine/core";

const ExperienceLoading = () => {
	return (
		<Center className="absolute top-0 left-0 grid place-items-center w-screen h-screen">
			LOADING HKAA EXPERIENCE...
		</Center>
	);
};

export default ExperienceLoading;
